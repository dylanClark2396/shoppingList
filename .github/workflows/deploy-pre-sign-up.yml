name: Deploy Pre-Sign-Up Lambda

on:
  push:
    branches:
      - main
    paths:
      - 'backend/lambda/pre-sign-up/**'
      - '.github/workflows/deploy-pre-sign-up.yml'

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies into package dir
        working-directory: backend/lambda/pre-sign-up
        run: pip install -r requirements.txt -t ./package/

      - name: Bundle Lambda zip
        working-directory: backend/lambda/pre-sign-up
        run: |
          cp lambda_function.py ./package/
          cd package && zip -r $GITHUB_WORKSPACE/pre-sign-up.zip .

      - name: Configure AWS Credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE }}
          aws-region: us-east-2

      - name: Upload zip to S3
        run: aws s3 cp pre-sign-up.zip s3://${{ secrets.DEPLOY_BUCKET }}/pre-sign-up.zip

      - name: Create or update Lambda function code
        env:
          LAMBDA_EXEC_ROLE: ${{ secrets.LAMBDA_EXEC_ROLE_ARN }}
          DEPLOY_BUCKET: ${{ secrets.DEPLOY_BUCKET }}
        run: |
          if aws lambda get-function --function-name pre-sign-up 2>/dev/null; then
            echo "Function exists — updating code..."
            aws lambda update-function-code \
              --function-name pre-sign-up \
              --s3-bucket "$DEPLOY_BUCKET" \
              --s3-key pre-sign-up.zip
          else
            echo "Function does not exist — creating..."
            aws lambda create-function \
              --function-name pre-sign-up \
              --runtime python3.12 \
              --handler lambda_function.handler \
              --role "$LAMBDA_EXEC_ROLE" \
              --code "S3Bucket=$DEPLOY_BUCKET,S3Key=pre-sign-up.zip"
          fi

      - name: Wait for function update to finish
        run: aws lambda wait function-updated --function-name pre-sign-up
